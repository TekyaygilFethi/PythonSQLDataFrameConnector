# Python SQL DataFrame Connector
This module allows you to create sql create and insertion queries for given dataframe. For now, it only works for PostgreSQL. Feel free to pull request to add new features!
